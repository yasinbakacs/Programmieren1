{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build (c)",
            "type": "shell",
            "command": "cc",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}