# Expected problems to find

The file contains several errors and weaknesses, including:

* a template-related compiler error
* an incorrect use of std::max
* wrong iterator usage with std::copy_if
* a type mismatch when inserting sensor readings into a vector of doubles
* a possible division by zero
* a hidden precision problem in calculate_average
* missing handling of empty vectors
* overly generic template code where a simpler function may be better